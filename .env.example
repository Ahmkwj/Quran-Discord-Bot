DISCORD_TOKEN=your_bot_token_here
OWNER_ID=your_discord_user_id
DEFAULT_VOLUME=80

# Optional: set GUILD_ID to register slash commands to one server only (instant, no delay)
# Leave unset to register globally (can take up to 1 hour to appear everywhere)
# GUILD_ID=your_discord_server_id
